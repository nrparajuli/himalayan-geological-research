document.addEventListener('DOMContentLoaded',()=>{const tabs=document.querySelectorAll('.tab'),rows=document.querySelectorAll('.journal-row'),search=document.getElementById('articleSearch');function filter(){const active=document.querySelector('.tab.active')?.dataset.filter||'all',q=(search?.value||'').toLowerCase();rows.forEach(r=>{const ok=(active==='all'||r.dataset.type===active)&&r.innerText.toLowerCase().includes(q);r.style.display=ok?'grid':'none'})}tabs.forEach(t=>t.addEventListener('click',()=>{tabs.forEach(x=>x.classList.remove('active'));t.classList.add('active');filter()}));if(search)search.addEventListener('input',filter)});